import 'package:flutter_test/flutter_test.dart';

void main() {
  test('App package is correctly configured', () {
    // Basic sanity test to verify the project compiles
    expect(1 + 1, equals(2));
  });
}
